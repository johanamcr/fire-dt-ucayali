streamlit==1.61.0
streamlit-folium==0.27.4
pandas==3.0.5
numpy==2.4.6
plotly==6.9.0
folium==0.20.0
requests==2.34.2
dbfread==2.0.7
pyarrow==24.0.0
matplotlib==3.11.1
seaborn==0.13.2
# Fixed: streamlit 1.61's gzip middleware is incompatible with starlette>=1.0
# (GZipResponder requires thread_minimum_size) -> every HTTP request returns 500.
# Keep starlette<1 and the matching uvicorn.
starlette==0.52.1
uvicorn==0.52.1
